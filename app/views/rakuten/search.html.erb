<!DOCTYPE html>
<html>
<%= form_tag :rakuten_search, method: :get do  %>
  <%= label_tag :keyword, 'キーワード検索' %>
  <%= text_field_tag 'keyword', params[:keyword] %>
  <%= submit_tag "検索" %>
<% end %>

<% if @tweets1.present? %>
  <h2>トレンド</h2>
  <ul>
    <% @tweets1.each_with_index do |tweet1, i| %>
    <div class="list">
      <li> <%= tweet1.name %> </li>
    </div>
    <% end %>
  </ul>
<% end %>

<% if @tweets.present? %>
  <h2>検索結果</h2>
  <ul>
    <% @tweets.each_with_index do |tweet, i| %>
    <div class="list">
      <li> <%= tweet.contents %> </li>
    </div>
    <% end %>
  </ul>
<% end %>


<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>価格</th>
      <th>画像</th>
    </tr>
  </thead>
  <tbody>
   <% if @items.present? %>
     <% @items.each do |item| %>
       <tr>
         <td><%= link_to item.name, "#{item.url}" %></td>
         <td align="right"><%= number_with_delimiter(item.price) %>円</td>
         <td><%= image_tag item["smallImageUrls"][0] %></td>
       </tr>
     <% end %>
   <% end %>
   </tbody>
</table>
</html>
